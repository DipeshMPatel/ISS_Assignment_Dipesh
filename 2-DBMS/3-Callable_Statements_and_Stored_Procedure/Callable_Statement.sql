-- A Callable Statement is used to call a Stored Procedure.
-- CALL procedure_name(parameters);
-- This can only be done after the procedure has been created.

CALL high_salaries();

CALL find_employee(3);